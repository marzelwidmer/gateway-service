apiVersion: apps/v1
kind: Deployment
metadata:
  name: gateway-service
spec:
  template:
    spec:
      volumes:
        - name: config
          configMap:
            name: gateway-service-config
      containers:
        - name: gateway-service
          volumeMounts:
            - mountPath: /config
              name: config