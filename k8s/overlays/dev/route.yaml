apiVersion: route.openshift.io/v1
kind: Route
metadata:
  labels:
    app: gateway-service
    env: dev
    stage: dev
  name: gateway-service
  namespace: dev
spec:
  host: gateway-service-dev.apps.c3smonkey.ch
  port:
    targetPort: 8080-8080
  to:
    kind: Service
    name: gateway-service
    weight: 100
  wildcardPolicy: None
status:
  ingress:
    - conditions:
      host: gateway-service-dev.apps.c3smonkey.ch
      routerName: router
      wildcardPolicy: None