spring:
  application.name: gateway
  cloud:
    kubernetes:
      reload:
        enabled: true
        mode: polling
        period: 5000

      discovery:
        enabled: true
      ribbon:
        enabled: true
        mode: service

